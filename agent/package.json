{
  "name": "teamworks-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Agent Host for TEAM WORKS — runs a ReAct loop over gemma2:9b and dispatches MCP tool calls to teamworks-mcp",
  "type": "module",
  "scripts": {
    "server": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "express": "^4.21.1"
  }
}
